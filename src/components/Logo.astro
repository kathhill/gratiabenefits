---
import logo from "../images/logo-higher-res.svg";

interface Props {
	width: string;
	height: string;
	class?: string;
}

const { width, height, class: className } = Astro.props;
---

<img
	width={width}
	height={height}
	class={className}
	src={logo.src}
	alt="Gratia Insurance Solutions"
/>
